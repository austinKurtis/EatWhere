<%= form_with(model: eventmember, local: true) do |form| %>
  <% if eventmember.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eventmember.errors.count, "error") %> prohibited this eventmember from being saved:</h2>

      <ul>
      <% eventmember.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :eventmember_user_id %>
  </div>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= form.number_field :restaurant_id, id: :eventmember_restaurant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
