<%= form_with(model: eventmember, local: true) do |form| %>

    <%= form.hidden_field :user_id, :value => current_user.id  %>
    <%= form.hidden_field :event_id, :value => current_user.id  %>
<% end %>
